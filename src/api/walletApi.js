// src/api/walletApi.js — wallets & payouts (extend when backend shares contract)
// import axiosClient from "./axiosClient";

/** Set false once real endpoints are wired in this file */
export const walletsPayoutsApiPending = true;
